// Copyright (c) 2020, Finbyz and contributors
// For license information, please see license.txt

frappe.ui.form.on('Payment Followup Remarks', {
	// refresh: function(frm) {

	// }
	// refresh: function(frm){
	// 	if (!frm.__is_local){
	// 		let route_history = frappe.route_history.reverse();
	// 		let route = []
	// 		if (route_history){
	// 			route = route_history.slice(-1).pop()
	// 		}
	// 		if (route[1] == 'Accounts Receivable Primary Customer'){
	// 			frappe.set_route(`#query-report/Accounts Receivable Primary Customer`);
	// 		}
	// 	}
	// }
});
